
<%= link_to 'Create work group', url_for(:controller => :wgs, :method => :create) %><br>
Workgroups under projects that you manage:<br>
<% @projects.each do |proj| %>
  <% WorkGroup.where(projid: proj.id).each do |work| %>
    |Project id: <%= work.projid %>| <%= work.name %> <%= work.budget %> 
    <%= link_to 'Delete', url_for(:controller => :wgs, :method => :destroy, :id => work.id) %>
    <br>
  <% end %>
<% end %>

<br>
Workgroups that you're a member of: <br>
<% WorkGroupMember.where(member: current_user['email']).each do |wgm| %>
  <% WorkGroup.where(id: wgm.wgid).each do |wg| %>
    <%= wg.name %> <%= wg.budget %><br>
  <% end %>
<% end %>

